CREATE TABLE IF NOT EXISTS COURSES(ID VARCHAR PRIMARY KEY NOT NULL,
                                   NAME VARCHAR NOT NULL,
                                   ORDER VARCHAR NOT NULL,
                                   TYPE VARCHAR NOT NULL,
                                   SPRITEURL VARCHAR NOT NULL,
                                   );